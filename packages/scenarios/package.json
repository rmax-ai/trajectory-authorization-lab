{
  "name": "@tacl/scenarios",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "scenario DSL + definitions",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^3.25.0"
  }
}
